"""External failure-attribution benchmark adapters (TRAIL, Who&When) plus the
shared `BenchmarkCase`/`BenchmarkAdapter` shape in `base.py`. See
`culprit.bench_score` for scoring and `registry.py` for the name -> adapter
lookup.
"""
